================================================================================
UNSAFE RUNTIME LOCALHOST FALLBACKS INVENTORY - nextjs-frontend
================================================================================
Generated: 2026-03-16
Purpose: Document unsafe localhost references that could cause issues in production

================================================================================
SECTION 1: UNSAFE LOCALHOST FALLBACKS IN RUNTIME CODE
================================================================================

The following files contain localhost:8000 fallbacks that could be executed at
runtime and would fail in production if environment variables are not set:

--------------------------------------------------------------------------------
SERVER ACTIONS (11 files)
All use: INTERNAL_API_BASE_URL || API_BASE_URL || NEXT_PUBLIC_API_BASE_URL || "http://localhost:8000"
--------------------------------------------------------------------------------
1. components/actions/ai-model-actions.ts (line 13)
2. components/actions/ai-catalog-actions.ts (line 13)
3. components/actions/ai-media-actions.ts (line 9)
4. components/actions/ai-scene-actions.ts (line 13)
5. components/actions/agent-actions.ts (line 13)
6. components/actions/ai-prompt-preset-actions.ts (line 18)
7. components/actions/builtin-agent-actions.ts (line 13)
8. components/actions/admin-rbac-actions.ts (line 13)
9. components/actions/credits-actions.ts (line 13)
10. components/actions/me-actions.ts (line 6)
11. components/actions/script-actions.ts (line 13)

RISK: These server actions run on the server-side but call external backend APIs.
If deployed to production without proper environment variables, requests will
attempt to connect to localhost:8000 which will fail.

--------------------------------------------------------------------------------
APP API ROUTES (82 files)
All use: INTERNAL_API_BASE_URL || API_BASE_URL || NEXT_PUBLIC_API_BASE_URL || "http://localhost:8000"
--------------------------------------------------------------------------------
List of 82 files - same as listed in task-1-url-inventory.txt

RISK: Next.js API routes that proxy to backend. If deployed without proper
environment variables, the proxy will fail.

================================================================================
SECTION 2: SPECIAL CASES WITH DIFFERENT PATTERNS
================================================================================

File: app/api/canvases/_proxy.ts (line 5)
Pattern: INTERNAL_API_BASE_URL || "http://localhost:8100"
RISK: Uses port 8100 (canvas service). Missing fallback to API_BASE_URL and
NEXT_PUBLIC_API_BASE_URL. If INTERNAL_API_BASE_URL is not set, will fail.

File: app/apiKeyService.ts (line 34)
Pattern: NEXT_PUBLIC_API_URL (different env var!) || "http://localhost:8000"
RISK: Uses NEXT_PUBLIC_API_URL instead of NEXT_PUBLIC_API_BASE_URL.
If that specific env var is not set, will default to localhost:8000.

================================================================================
SECTION 3: NON-RUNTIME REFERENCES (LABELED FOR CLARITY)
================================================================================

File: lib/utils.ts (line 112)
Content: "无法连接后端 API（默认 http://127.0.0.1:8000）。请先启动 fastapi_backend，
         或设置 NEXT_PUBLIC_API_BASE_URL/API_BASE_URL。"
Type: User-facing ERROR MESSAGE (not runtime logic)
Status: SAFE - This is just a helpful error message for developers

File: scripts/verify-login.mjs (lines 3-7)
Pattern: API_BASE_URL → NEXT_PUBLIC_API_BASE_URL → INTERNAL_API_BASE_URL → "http://127.0.0.1:8000"
Type: Build/verification script
Status: ACCEPTABLE - Scripts are expected to have dev fallbacks

================================================================================
SECTION 4: COMPARISON - CENTRALIZED VS DISTRIBUTED
================================================================================

CENTRALIZED (lib/clientConfig.ts):
- Browser: baseURL = "" (relative path - SAFE for production)
- Server: Uses INTERNAL_API_BASE_URL → API_BASE_URL → NEXT_PUBLIC_API_BASE_URL
         → (dev fallback: http://127.0.0.1:8000 only in development)
- Has proper error handling: throws if no baseURL in production

DISTRIBUTED (93 files):
- All use local getApiBaseUrl() functions
- All default to http://localhost:8000 or http://localhost:8100
- NO environment-specific checks (e.g., NODE_ENV === "development")
- NO error handling for missing configuration

================================================================================
SECTION 5: SUMMARY OF UNSAFE PATTERNS
================================================================================

CRITICAL ISSUES:
1. 93 files have hardcoded localhost:8000 fallbacks without NODE_ENV checks
2. 1 file (canvases/_proxy.ts) has different port (8100) and missing fallbacks
3. 1 file uses wrong env var name (NEXT_PUBLIC_API_URL vs NEXT_PUBLIC_API_BASE_URL)

IMPACT:
- Production deployments will fail if environment variables are not set
- Developers may not realize fallback is happening
- Debugging is difficult because failures appear silently

RECOMMENDATION:
- Refactor to use centralized clientConfig.ts
- Or add NODE_ENV checks to prevent localhost fallback in production
- Or ensure environment variables are always set in all deployment environments

================================================================================
