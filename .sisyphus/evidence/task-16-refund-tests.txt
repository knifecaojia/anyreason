Task 16 refund traceability assertions

Refund coverage now verifies:
- reason == "agent.refund"
- trace_type == "agent"
- operation_display == "智能体: refund-agent"
- is_refund == true
- linked_event_id matches the original agent.consume transaction
- meta.refunded == true
- meta.original_transaction_id points to the consume transaction id
- meta.original_delta records the original debit amount
- meta.error_code == "app_error"

Admin history coverage also confirms the original consume row remains visible with:
- is_refund == false
- meta.refunded == false
