# Task 18: Routing Scope Verification Evidence

## Purpose
Verify that queue code path is ONLY invoked for media/video flows.

## Routing Verification

### Source Code Analysis

#### Non-Media Paths (chat_text):
```python
# NOT using allow_queue=True
# NOT using skip_slot_acquisition
# Uses default allow_queue=False
cfg, cfg_id, resolved_binding_key, _ = await self._resolve_model_config(
    db=db,
    category="text",
    binding_key=binding_key,
    model_config_id=model_config_id,
    default_binding_key="chatbox",
)
# allow_queue defaults to False → FAIL FAST on slot exhaustion
```

#### Non-Media Paths (chat_text_stream):
```python
# NOT using allow_queue=True
# Uses default allow_queue=False
cfg, cfg_id, resolved_binding_key, _ = await self._resolve_model_config(
    db=db,
    category="text",
    binding_key=binding_key,
    model_config_id=model_config_id,
    default_binding_key="chatbox",
)
# allow_queue defaults to False → FAIL FAST on slot exhaustion
```

#### Non-Media Paths (generate_media):
```python
# NOT using allow_queue=True
# Uses default allow_queue=False
cfg, cfg_id, resolved_binding_key, _ = await self._resolve_model_config(
    db=db,
    category=category,
    binding_key=binding_key,
    model_config_id=model_config_id,
    default_binding_key=category,
)
# allow_queue defaults to False → FAIL FAST on slot exhaustion
```

#### Queueable Media Path (submit_media_async):
```python
# USES allow_queue=True
cfg, cfg_id, resolved_binding_key, slot_result = await self._resolve_model_config(
    db=db,
    category=category,
    binding_key=binding_key,
    model_config_id=model_config_id,
    default_binding_key=category,
    allow_queue=True,  # ← QUEUE on slot exhaustion
)
```

## Conclusion
Queue scheduler is ONLY invoked when:
- `allow_queue=True` is explicitly passed
- ONLY submit_media_async passes allow_queue=True

Non-media paths NEVER invoke queue code.
