# 言之有理（anyreason） 数据库设计说明书 (Database Design Specification)

> **文档状态：** `Draft`
>
> **当前版本：** `v4.0`
>
> **最后更新：** `2026-02-15`
>
> **维护人：** `架构组`

---

## 1. 设计规约 (Design Conventions)

### 1.1 命名规范 (Naming Convention)

* **表名** ：使用 **复数形式** ，小写下划线分隔（`snake_case`）。例：`projects`, `video_prompts`。
* **主键** ：统一命名为 `id`，类型为 `UUID`。
* **外键** ：`[单数表名]_id`。例：`project_id`, `scene_id`。
* **索引** ：`idx_[表名]_[字段名]` 或 `uq_[表名]_[字段名]` (唯一索引)。
* **时间字段** ：统一使用 `created_at` (创建时间) 和 `updated_at` (更新时间)。

### 1.2 核心策略 (Core Strategy)

* **ID 双轨制** ：
  * **物理主键 (PK)** ：使用 `UUID v4`，用于系统内部关联，确保全局唯一且无序。
  * **业务编码 (Code)** ：对外展示/人类阅读字段（如 `role.name`、`permission.code`），并以唯一约束保证一致性。
* **JSONB 使用原则** ：
  * 易变结构存储为 JSONB（如 `audit_logs.meta`）。
  * 核心查询字段必须提升为实体列（如 `audit_logs.action`、`permissions.code`）。
* **软删除 (Soft Delete)** ：权限与审计模块当前不启用软删除；如未来涉及合规与恢复需求，可为核心表增加 `deleted_at`。

---

## 2. 实体关系图 (ER Diagram)

```
erDiagram
    USER ||--o{ USER_ROLE : "has"
    ROLE ||--o{ USER_ROLE : "assigned_to"
    ROLE ||--o{ ROLE_PERMISSION : "grants"
    PERMISSION ||--o{ ROLE_PERMISSION : "included_in"
    USER ||--o{ AUDIT_LOG : "acts_as (nullable)"

    USER_ROLE {
        uuid id PK
        uuid user_id FK
        uuid role_id FK
        timestamp created_at
    }

    ROLE_PERMISSION {
        uuid id PK
        uuid role_id FK
        uuid permission_id FK
        timestamp created_at
    }
```

---

## 3. 物理模型详解 (Physical Schema)

### 3.1 角色表 (roles)

| **字段名** | **类型** | **必填** | **默认值** | **描述/约束** |
| --- | --- | --- | --- | --- |
| `id` | `UUID` | ✅ | `gen_random_uuid()` | 物理主键 |
| `name` | `VARCHAR(64)` | ✅ | - | 角色名，唯一 |
| `description` | `TEXT` | - | - | 角色描述 |
| `created_at` | `TIMESTAMP` | ✅ | `now()` | 创建时间 |
| `updated_at` | `TIMESTAMP` | ✅ | `now()` | 更新时间 |

索引：
* `UNIQUE INDEX uq_roles_name (name)`
* `INDEX idx_roles_name (name)`

---

### 3.2 权限表 (permissions)

| **字段名** | **类型** | **必填** | **默认值** | **描述/约束** |
| --- | --- | --- | --- | --- |
| `id` | `UUID` | ✅ | `gen_random_uuid()` | 物理主键 |
| `code` | `VARCHAR(128)` | ✅ | - | 权限编码，唯一（建议 `system.users` 这种点分层级） |
| `description` | `TEXT` | - | - | 权限描述 |
| `created_at` | `TIMESTAMP` | ✅ | `now()` | 创建时间 |
| `updated_at` | `TIMESTAMP` | ✅ | `now()` | 更新时间 |

索引：
* `UNIQUE INDEX uq_permissions_code (code)`
* `INDEX idx_permissions_code (code)`

---

### 3.3 用户-角色关联表 (user_roles)

| **字段名** | **类型** | **必填** | **默认值** | **描述/约束** |
| --- | --- | --- | --- | --- |
| `id` | `UUID` | ✅ | `gen_random_uuid()` | 物理主键 |
| `user_id` | `UUID` | ✅ | - | 关联用户（FK -> `user.id`） |
| `role_id` | `UUID` | ✅ | - | 关联角色（FK -> `roles.id`） |
| `created_at` | `TIMESTAMP` | ✅ | `now()` | 创建时间 |

索引：
* `UNIQUE INDEX uq_user_roles_user_role (user_id, role_id)`
* `INDEX idx_user_roles_user (user_id)`
* `INDEX idx_user_roles_role (role_id)`

---

### 3.4 角色-权限关联表 (role_permissions)

| **字段名** | **类型** | **必填** | **默认值** | **描述/约束** |
| --- | --- | --- | --- | --- |
| `id` | `UUID` | ✅ | `gen_random_uuid()` | 物理主键 |
| `role_id` | `UUID` | ✅ | - | 关联角色（FK -> `roles.id`） |
| `permission_id` | `UUID` | ✅ | - | 关联权限（FK -> `permissions.id`） |
| `created_at` | `TIMESTAMP` | ✅ | `now()` | 创建时间 |

索引：
* `UNIQUE INDEX uq_role_permissions_role_permission (role_id, permission_id)`
* `INDEX idx_role_permissions_role (role_id)`
* `INDEX idx_role_permissions_permission (permission_id)`

---

### 3.5 系统审计日志表 (audit_logs)

| **字段名** | **类型** | **必填** | **默认值** | **描述/约束** |
| --- | --- | --- | --- | --- |
| `id` | `UUID` | ✅ | `gen_random_uuid()` | 物理主键 |
| `actor_user_id` | `UUID` | - | - | 操作人（FK -> `user.id`，可为空） |
| `action` | `VARCHAR(128)` | ✅ | - | 事件动作（如 `role.create`、`user.roles.set`） |
| `resource_type` | `VARCHAR(64)` | - | - | 资源类型（如 `role`、`user`） |
| `resource_id` | `UUID` | - | - | 资源 ID（如角色/用户 UUID） |
| `success` | `BOOLEAN` | ✅ | `true` | 成功标记 |
| `request_id` | `VARCHAR(64)` | - | - | 请求 ID（用于串联应用日志与审计记录） |
| `ip` | `VARCHAR(64)` | - | - | 客户端 IP |
| `user_agent` | `TEXT` | - | - | UA |
| `meta` | `JSONB` | ✅ | `{}` | 额外信息（不存敏感数据） |
| `created_at` | `TIMESTAMP` | ✅ | `now()` | 创建时间 |

索引：
* `INDEX idx_audit_logs_actor (actor_user_id)`
* `INDEX idx_audit_logs_created_at (created_at)`
* `INDEX idx_audit_logs_action (action)`

---

### 3.6 剧本源文件表 (scripts)

说明：保存“原始剧本文件”的对象存储定位信息（MinIO bucket/key）与基础元数据；实际文件内容不落库。

| **字段名** | **类型** | **必填** | **默认值** | **描述/约束** |
| --- | --- | --- | --- | --- |
| `id` | `UUID` | ✅ | `gen_random_uuid()` | 物理主键 |
| `owner_id` | `UUID` | ✅ | - | 归属用户（FK -> `user.id`，on delete cascade） |
| `title` | `VARCHAR(255)` | ✅ | - | 剧本标题 |
| `description` | `TEXT` | - | - | 简述/梗概 |
| `minio_bucket` | `VARCHAR(255)` | ✅ | - | MinIO Bucket |
| `minio_key` | `TEXT` | ✅ | - | MinIO Object Key |
| `original_filename` | `VARCHAR(255)` | ✅ | - | 原始文件名（用于下载时回写） |
| `content_type` | `VARCHAR(128)` | - | - | MIME 类型 |
| `size_bytes` | `INT` | ✅ | `0` | 文件大小（字节） |
| `created_at` | `TIMESTAMP` | ✅ | `now()` | 创建时间 |

索引：
* `INDEX idx_scripts_owner (owner_id)`
* `INDEX idx_scripts_owner_created_at (owner_id, created_at)`

---

### 3.7 剧集表 (episodes)

说明：保存“按集结构化”的剧本拆分结果；`script_full_text` 用于存储该集完整文本。

| **字段名** | **类型** | **必填** | **默认值** | **描述/约束** |
| --- | --- | --- | --- | --- |
| `id` | `UUID` | ✅ | `gen_random_uuid()` | 物理主键 |
| `project_id` | `UUID` | - | - | 归属项目（FK -> `projects.id`） |
| `episode_code` | `VARCHAR(20)` | ✅ | - | 业务编码（建议 `EP001`） |
| `episode_number` | `INT` | ✅ | - | 集序号 |
| `title` | `VARCHAR(255)` | - | - | 标题 |
| `summary` | `TEXT` | - | - | 摘要 |
| `script_full_text` | `TEXT` | - | - | 该集拆分后的剧本文本全文 |
| `episode_doc_node_id` | `UUID` | - | - | Episode 正文 Markdown（SSOT）文档指针（FK -> `file_nodes.id`，on delete set null） |
| `storyboard_root_node_id` | `UUID` | - | - | 故事板结果根目录（FK -> `file_nodes.id`，on delete set null） |
| `asset_root_node_id` | `UUID` | - | - | 资产结果根目录（FK -> `file_nodes.id`，on delete set null） |
| `word_count` | `INT` | ✅ | `0` | 字符/词统计（当前以非空白字符计数） |
| `start_line` | `INT` | - | - | 在拆分基准文本中的起始行号 |
| `end_line` | `INT` | - | - | 在拆分基准文本中的结束行号 |
| `status` | `VARCHAR(20)` | ✅ | `'pending'` | 状态（解析/制作流水线可复用） |
| `stage_tag` | `VARCHAR(50)` | - | - | 阶段标签 |
| `created_at` | `TIMESTAMP` | ✅ | `now()` | 创建时间 |

索引：
* `UNIQUE INDEX uq_episodes_project_episode_code (project_id, episode_code)`
* `INDEX idx_episodes_episode_doc_node_id (episode_doc_node_id)`

---

### 3.7.1 文件节点表 (file_nodes)

说明：虚拟文件系统（VFS）节点表，用于承载“故事板结果/资产结果”的 **Markdown 文件化存储**。数据库仅保存节点元数据（目录结构、文件名、MinIO 指针），内容存入 MinIO（例如 `anyreason-vfs` bucket）。

| **字段名** | **类型** | **必填** | **默认值** | **描述/约束** |
| --- | --- | --- | --- | --- |
| `id` | `UUID` | ✅ | `gen_random_uuid()` | 物理主键 |
| `workspace_id` | `UUID` | - | - | 工作区（预留） |
| `project_id` | `UUID` | - | - | 归属项目（当前与 `scripts.id` 同 id 的 `projects.id` 绑定） |
| `parent_id` | `UUID` | - | - | 父节点（FK -> `file_nodes.id`） |
| `name` | `VARCHAR(255)` | ✅ | - | 文件/文件夹名称 |
| `is_folder` | `BOOLEAN` | ✅ | `false` | 是否为目录 |
| `minio_bucket` | `VARCHAR(255)` | - | - | 文件存储 bucket（目录为空） |
| `minio_key` | `TEXT` | - | - | 文件存储 key（目录为空） |
| `content_type` | `VARCHAR(128)` | - | - | MIME 类型（如 `text/markdown`） |
| `size_bytes` | `BIGINT` | ✅ | `0` | 文件大小（字节） |
| `created_by` | `UUID` | - | - | 创建人（FK -> `user.id`） |
| `created_at` | `TIMESTAMP` | ✅ | `now()` | 创建时间 |
| `updated_at` | `TIMESTAMP` | ✅ | `now()` | 更新时间 |

索引建议：
* `INDEX idx_file_nodes_parent (parent_id)`
* `INDEX idx_file_nodes_project_parent (project_id, parent_id)`

---

### 3.7.2 分镜表 (storyboards)

说明：分镜统一表（已扁平化），兼容“分场占位”（`shot_number=1`）与“多镜头列表”。同一 `episode_id` 内 `shot_code` 唯一。

| **字段名** | **类型** | **必填** | **默认值** | **描述/约束** |
| --- | --- | --- | --- | --- |
| `id` | `UUID` | ✅ | `gen_random_uuid()` | 物理主键 |
| `episode_id` | `UUID` | - | - | 归属剧集（FK -> `episodes.id`，on delete cascade） |
| `shot_code` | `VARCHAR(50)` | ✅ | - | 业务编码（建议 `EP001_SC01_SH01`） |
| `shot_number` | `INT` | ✅ | - | 镜头序号（建议在同一 scene 下递增） |
| `scene_code` | `VARCHAR(50)` | - | - | 场次编码（建议 `EP001_SC01`） |
| `scene_number` | `INT` | - | - | 场次序号 |
| `shot_type` | `VARCHAR(20)` | - | - | 景别 |
| `camera_move` | `VARCHAR(50)` | - | - | 运镜 |
| `narrative_function` | `VARCHAR(20)` | - | - | 叙事功能 |
| `location` | `VARCHAR(100)` | - | - | 地点 |
| `location_type` | `VARCHAR(10)` | - | - | 内/外/内外 |
| `time_of_day` | `VARCHAR(50)` | - | - | 时间（如 DAY/NIGHT） |
| `description` | `TEXT` | - | - | 画面描述 |
| `dialogue` | `TEXT` | - | - | 台词 |
| `duration_estimate` | `NUMERIC(5,2)` | - | - | 时长估计 |
| `active_assets` | `JSONB` | ✅ | `[]` | 出镜资产引用（名称/关键词数组） |
| `created_at` | `TIMESTAMP` | ✅ | `now()` | 创建时间 |

约束与索引：
* `UNIQUE uq_storyboards_episode_shot_code (episode_id, shot_code)`
* `INDEX idx_storyboards_episode (episode_id)`
* `INDEX idx_storyboards_scene_group (episode_id, scene_number)`

---

### 3.7.3 视频提示词表 (video_prompts)

说明：分镜生视频提示词的落库表，`storyboard_id` 指向具体镜头。

| **字段名** | **类型** | **必填** | **默认值** | **描述/约束** |
| --- | --- | --- | --- | --- |
| `id` | `UUID` | ✅ | `gen_random_uuid()` | 物理主键 |
| `storyboard_id` | `UUID` | - | - | 关联分镜（FK -> `storyboards.id`，on delete cascade） |
| `prompt_main` | `TEXT` | - | - | 主提示词 |
| `negative_prompt` | `TEXT` | - | - | 负面提示词 |
| `style_model` | `VARCHAR(50)` | - | - | 风格/模型标签（可选） |
| `aspect_ratio` | `VARCHAR(10)` | - | - | 画幅比例（如 `9:16`） |
| `character_prompts` | `JSONB` | ✅ | `[]` | 角色独立提示词 |
| `camera_settings` | `JSONB` | ✅ | `{}` | 摄像机/镜头参数 |
| `duration` | `NUMERIC(5,2)` | - | - | 生成时长（秒） |
| `generation_notes` | `TEXT` | - | - | 生成备注 |
| `created_at` | `TIMESTAMP` | ✅ | `now()` | 创建时间 |

索引建议：
* `INDEX idx_video_prompts_storyboard (storyboard_id)`

---

### 3.7.4 生图提示词表 (image_prompts)

说明：分镜生图提示词的落库表，结构与 `video_prompts` 保持对齐，便于在工具侧复用。

| **字段名** | **类型** | **必填** | **默认值** | **描述/约束** |
| --- | --- | --- | --- | --- |
| `id` | `UUID` | ✅ | `gen_random_uuid()` | 物理主键 |
| `storyboard_id` | `UUID` | - | - | 关联分镜（FK -> `storyboards.id`，on delete cascade） |
| `prompt_main` | `TEXT` | - | - | 主提示词 |
| `negative_prompt` | `TEXT` | - | - | 负面提示词 |
| `style_model` | `VARCHAR(50)` | - | - | 风格/模型标签（可选） |
| `aspect_ratio` | `VARCHAR(10)` | - | - | 画幅比例（如 `9:16`） |
| `character_prompts` | `JSONB` | ✅ | `[]` | 角色独立提示词 |
| `camera_settings` | `JSONB` | ✅ | `{}` | 摄像机/镜头参数 |
| `generation_notes` | `TEXT` | - | - | 生成备注 |
| `created_at` | `TIMESTAMP` | ✅ | `now()` | 创建时间 |

索引建议：
* `INDEX idx_image_prompts_storyboard (storyboard_id)`

---

### 3.8 资产绑定表 (asset_bindings)

说明：资产可绑定到剧集/分场/分镜三层之一；通过约束保证每条绑定仅指向一个层级目标。

| **字段名** | **类型** | **必填** | **默认值** | **描述/约束** |
| --- | --- | --- | --- | --- |
| `id` | `UUID` | ✅ | `gen_random_uuid()` | 物理主键 |
| `asset_entity_id` | `UUID` | ✅ | - | 资产实体（FK -> `assets.id`，on delete cascade） |
| `asset_variant_id` | `UUID` | - | - | 资产变体（FK -> `asset_variants.id`，on delete set null） |
| `episode_id` | `UUID` | - | - | 绑定目标：剧集（FK -> `episodes.id`） |
| `scene_id` | `UUID` | - | - | 绑定目标：分场（FK -> `scenes.id`） |
| `shot_id` | `UUID` | - | - | 绑定目标：分镜（FK -> `shots.id`） |
| `state` | `JSONB` | ✅ | `{}` | 绑定状态（镜头内摆放/服饰差异等扩展） |
| `created_at` | `TIMESTAMP` | ✅ | `now()` | 创建时间 |

约束：
* `CHECK ck_asset_bindings_single_target`：`episode_id/scene_id/shot_id` 恰好一个非空
* `UNIQUE uq_asset_bindings_episode_asset (episode_id, asset_entity_id)`
* `UNIQUE uq_asset_bindings_scene_asset (scene_id, asset_entity_id)`
* `UNIQUE uq_asset_bindings_shot_asset (shot_id, asset_entity_id)`

索引：
* `INDEX idx_asset_bindings_asset (asset_entity_id)`
* `INDEX idx_asset_bindings_episode (episode_id)`
* `INDEX idx_asset_bindings_scene (scene_id)`
* `INDEX idx_asset_bindings_shot (shot_id)`

---

### 3.9 AI 模型配置表 (ai_model_configs)

说明：平台级共享的模型配置表（文本/图片/视频三类）。`api_key` 仅以加密形式存储，接口不会返回明文。

| **字段名** | **类型** | **必填** | **默认值** | **描述/约束** |
| --- | --- | --- | --- | --- |
| `id` | `UUID` | ✅ | `gen_random_uuid()` | 物理主键 |
| `category` | `VARCHAR(16)` | ✅ | - | 类别（`text/image/video`） |
| `manufacturer` | `VARCHAR(64)` | ✅ | - | 厂商（如 `openai/kling`） |
| `model` | `VARCHAR(128)` | ✅ | - | 模型名（如 `gpt-4o-mini/kling-image-o1`） |
| `base_url` | `TEXT` | - | - | 可选 Base URL（OpenAI-compatible 或厂商特定地址） |
| `encrypted_api_key` | `BYTEA` | - | - | API Key 加密存储（可为空） |
| `enabled` | `BOOLEAN` | ✅ | `true` | 是否启用 |
| `sort_order` | `INT` | ✅ | `0` | 排序（同类内升序） |
| `created_at` | `TIMESTAMP` | ✅ | `now()` | 创建时间 |
| `updated_at` | `TIMESTAMP` | ✅ | `now()` | 更新时间 |

约束：
* `UNIQUE uq_ai_model_configs_category_manu_model (category, manufacturer, model)`
* `CHECK ck_ai_model_configs_category`：`category` 必须为 `text/image/video`

索引：
* `INDEX idx_ai_model_configs_category (category)`
* `INDEX idx_ai_model_configs_enabled (enabled)`
* `INDEX idx_ai_model_configs_sort (category, sort_order)`

---

### 3.10 AI 模型用途绑定表 (ai_model_bindings)

说明：为“用途 key”绑定一个默认模型配置（可为空表示未绑定）。用途 key 可用于前端工具与后端任务统一路由。

| **字段名** | **类型** | **必填** | **默认值** | **描述/约束** |
| --- | --- | --- | --- | --- |
| `id` | `UUID` | ✅ | `gen_random_uuid()` | 物理主键 |
| `key` | `VARCHAR(64)` | ✅ | - | 用途 key（如 `chatbox/image/video`） |
| `category` | `VARCHAR(16)` | ✅ | - | 类别（`text/image/video`） |
| `ai_model_config_id` | `UUID` | - | - | 绑定的模型配置（FK -> `ai_model_configs.id`，on delete set null） |
| `created_at` | `TIMESTAMP` | ✅ | `now()` | 创建时间 |
| `updated_at` | `TIMESTAMP` | ✅ | `now()` | 更新时间 |

约束：
* `UNIQUE uq_ai_model_bindings_key (key)`
* `CHECK ck_ai_model_bindings_category`：`category` 必须为 `text/image/video`

索引：
* `INDEX idx_ai_model_bindings_category (category)`
* `INDEX idx_ai_model_bindings_model_config (ai_model_config_id)`

---

### 3.11 AI 用量事件表 (ai_usage_events)

说明：记录平台内 AI 调用的用量与错误信息，用于审计与运营分析（不存明文密钥/敏感内容）。

| **字段名** | **类型** | **必填** | **默认值** | **描述/约束** |
| --- | --- | --- | --- | --- |
| `id` | `UUID` | ✅ | `gen_random_uuid()` | 物理主键 |
| `user_id` | `UUID` | - | - | 归属用户（FK -> `user.id`，on delete set null） |
| `category` | `VARCHAR(16)` | ✅ | - | 类别（`text/image/video`） |
| `binding_key` | `VARCHAR(64)` | - | - | 用途 key（若使用用途绑定调用） |
| `ai_model_config_id` | `UUID` | - | - | 模型配置（FK -> `ai_model_configs.id`，on delete set null） |
| `cost_credits` | `INT` | ✅ | `0` | 本次调用消耗积分 |
| `latency_ms` | `INT` | - | - | 调用耗时（毫秒，预留） |
| `error_code` | `VARCHAR(64)` | - | - | 错误码（预留） |
| `raw_payload` | `JSONB` | ✅ | `{}` | 原始结果/错误信息（脱敏后） |
| `created_at` | `TIMESTAMP` | ✅ | `now()` | 创建时间 |

约束：
* `CHECK ck_ai_usage_events_category`：`category` 必须为 `text/image/video`

索引：
* `INDEX idx_ai_usage_events_user (user_id)`
* `INDEX idx_ai_usage_events_created_at (created_at)`
* `INDEX idx_ai_usage_events_category (category)`
* `INDEX idx_ai_usage_events_binding_key (binding_key)`

---

### 3.12 异步任务表 (tasks)

说明：用于承载平台内“耗时/异步”业务（如 AI 分镜生成、结构化解析等）的任务元数据与最终结果。任务调度可交给队列系统（如 Redis），但任务状态与结果必须落库以支持回溯与审计。

| **字段名** | **类型** | **必填** | **默认值** | **描述/约束** |
| --- | --- | --- | --- | --- |
| `id` | `UUID` | ✅ | `gen_random_uuid()` | 物理主键 |
| `user_id` | `UUID` | ✅ | - | 归属用户（FK -> `user.id`，on delete cascade） |
| `type` | `VARCHAR(64)` | ✅ | - | 任务类型（如 `scene_storyboard_preview`） |
| `status` | `VARCHAR(16)` | ✅ | `'queued'` | 状态（见枚举字典） |
| `progress` | `INT` | ✅ | `0` | 进度（0-100） |
| `entity_type` | `VARCHAR(32)` | - | - | 关联业务实体类型（如 `scene` / `episode`） |
| `entity_id` | `UUID` | - | - | 关联业务实体 ID |
| `input_json` | `JSONB` | ✅ | `{}` | 输入参数（业务侧自定义结构） |
| `result_json` | `JSONB` | ✅ | `{}` | 执行结果（业务侧自定义结构） |
| `error` | `TEXT` | - | - | 错误信息（失败时写入，禁止包含敏感信息） |
| `created_at` | `TIMESTAMP` | ✅ | `now()` | 创建时间 |
| `updated_at` | `TIMESTAMP` | ✅ | `now()` | 更新时间 |
| `started_at` | `TIMESTAMP` | - | - | 开始执行时间 |
| `finished_at` | `TIMESTAMP` | - | - | 执行结束时间 |

约束：
* `CHECK ck_tasks_status`：`status` 必须为 `queued/running/succeeded/failed/canceled`
* `CHECK ck_tasks_progress`：`progress` 必须在 `0..100` 范围

索引：
* `INDEX idx_tasks_user (user_id)`
* `INDEX idx_tasks_user_status (user_id, status)`
* `INDEX idx_tasks_entity (entity_type, entity_id)`
* `INDEX idx_tasks_created_at (created_at)`

---

### 3.13 异步任务事件表 (task_events)

说明：记录任务生命周期内的重要事件（如状态变更、阶段进度、完成/失败原因），用于前端回溯与审计。

| **字段名** | **类型** | **必填** | **默认值** | **描述/约束** |
| --- | --- | --- | --- | --- |
| `id` | `UUID` | ✅ | `gen_random_uuid()` | 物理主键 |
| `task_id` | `UUID` | ✅ | - | 关联任务（FK -> `tasks.id`，on delete cascade） |
| `event_type` | `VARCHAR(32)` | ✅ | - | 事件类型（如 `status_changed` / `progress` / `completed`） |
| `payload` | `JSONB` | ✅ | `{}` | 事件载荷（结构由事件类型决定） |
| `created_at` | `TIMESTAMP` | ✅ | `now()` | 创建时间 |

索引：
* `INDEX idx_task_events_task (task_id)`
* `INDEX idx_task_events_task_created_at (task_id, created_at)`

---

### 3.14 积分账户表 (user_credit_accounts)

说明：每个用户一条记录，维护积分余额；所有增减通过 `credit_transactions` 追溯。

| **字段名** | **类型** | **必填** | **默认值** | **描述/约束** |
| --- | --- | --- | --- | --- |
| `user_id` | `UUID` | ✅ | - | 归属用户（PK, FK -> `user.id`，on delete cascade） |
| `balance` | `INT` | ✅ | `0` | 当前积分余额 |
| `created_at` | `TIMESTAMP` | ✅ | `now()` | 创建时间 |
| `updated_at` | `TIMESTAMP` | ✅ | `now()` | 更新时间 |

约束：
* `CHECK ck_user_credit_accounts_balance_nonneg`：`balance >= 0`

---

### 3.15 积分流水表 (credit_transactions)

说明：积分变更流水，记录变更原因、操作者与变更前后余额。

| **字段名** | **类型** | **必填** | **默认值** | **描述/约束** |
| --- | --- | --- | --- | --- |
| `id` | `UUID` | ✅ | `gen_random_uuid()` | 物理主键 |
| `user_id` | `UUID` | ✅ | - | 归属用户（FK -> `user.id`，on delete cascade） |
| `delta` | `INT` | ✅ | - | 变更增量（可正可负） |
| `balance_after` | `INT` | ✅ | - | 变更后余额 |
| `reason` | `VARCHAR(64)` | ✅ | - | 变更原因（如 `agent.consume` / `agent.refund`） |
| `actor_user_id` | `UUID` | - | - | 操作者（FK -> `user.id`，on delete set null） |
| `meta` | `JSONB` | ✅ | `{}` | 额外信息（禁止包含敏感数据） |
| `created_at` | `TIMESTAMP` | ✅ | `now()` | 创建时间 |

索引：
* `INDEX idx_credit_transactions_user_created_at (user_id, created_at)`

---

### 3.16 Agent 表 (agents)

说明：可配置的业务 Agent（文本/图片/视频）。Agent 直接绑定到一个 `ai_model_configs` 记录，运行时按该配置进行调用并按 `credits_per_call` 扣积分。

| **字段名** | **类型** | **必填** | **默认值** | **描述/约束** |
| --- | --- | --- | --- | --- |
| `id` | `UUID` | ✅ | `gen_random_uuid()` | 物理主键 |
| `name` | `VARCHAR(128)` | ✅ | - | Agent 名称，唯一 |
| `category` | `VARCHAR(16)` | ✅ | - | 类别（`text/image/video`） |
| `purpose` | `VARCHAR(32)` | ✅ | `'general'` | 用途（如 `storyboard_extraction/asset_extraction/.../general`） |
| `ai_model_config_id` | `UUID` | ✅ | - | 模型配置（FK -> `ai_model_configs.id`，on delete restrict） |
| `capabilities` | `JSONB` | ✅ | `[]` | 能力集合（如 `["text"]` / `["image"]` / `["video"]`） |
| `system_prompt` | `TEXT` | - | - | 系统提示词 |
| `user_prompt_template` | `TEXT` | - | - | 用户提示词模板（如 `{input}`） |
| `credits_per_call` | `INT` | ✅ | `0` | 单次调用消耗积分 |
| `enabled` | `BOOLEAN` | ✅ | `true` | 是否启用 |
| `created_at` | `TIMESTAMP` | ✅ | `now()` | 创建时间 |
| `updated_at` | `TIMESTAMP` | ✅ | `now()` | 更新时间 |

约束：
* `UNIQUE uq_agents_name (name)`
* `CHECK ck_agents_category`：`category` 必须为 `text/image/video`
* `CHECK ck_agents_purpose`：`purpose` 必须为预置用途集合
* `CHECK ck_agents_credits_per_call_nonneg`：`credits_per_call >= 0`

索引：
* `INDEX idx_agents_category (category)`
* `INDEX idx_agents_purpose (purpose)`
* `INDEX idx_agents_enabled (enabled)`
* `INDEX idx_agents_model_config (ai_model_config_id)`

---

### 3.17 内置专家 Agent 表 (builtin_agents)

说明：平台级内置专家 Agent（跨租户共享），使用 `agent_code` 作为对外稳定标识，物理主键为 UUID。默认模型通过 `default_ai_model_config_id` 指定（可后续扩展为按用户等级/绑定 key 选择）。

| **字段名** | **类型** | **必填** | **默认值** | **描述/约束** |
| --- | --- | --- | --- | --- |
| `id` | `UUID` | ✅ | `gen_random_uuid()` | 物理主键 |
| `agent_code` | `VARCHAR(64)` | ✅ | - | 业务编码（如 `script_expert`），唯一 |
| `name` | `VARCHAR(128)` | ✅ | - | 展示名称 |
| `description` | `TEXT` | - | - | 描述 |
| `category` | `VARCHAR(32)` | ✅ | - | 分类（如 `script/episode/asset/scene/vfx/storyboard`） |
| `default_ai_model_config_id` | `UUID` | - | - | 默认模型配置（FK -> `ai_model_configs.id`，on delete set null） |
| `tools` | `JSONB` | ✅ | `[]` | 工具 code 列表（后续可替换为关联表） |
| `created_at` | `TIMESTAMP` | ✅ | `now()` | 创建时间 |
| `updated_at` | `TIMESTAMP` | ✅ | `now()` | 更新时间 |

约束：
* `UNIQUE uq_builtin_agents_agent_code (agent_code)`

索引：
* `INDEX idx_builtin_agents_category (category)`
* `INDEX idx_builtin_agents_model_config (default_ai_model_config_id)`

---

### 3.18 内置专家提示词版本表 (builtin_agent_prompt_versions)

说明：内置专家 Agent 的提示词版本链。`version` 在同一 `builtin_agent_id` 下递增；默认版本通过 `is_default=true` 标记（同一 Agent 下最多一个默认版本）。\n

| **字段名** | **类型** | **必填** | **默认值** | **描述/约束** |
| --- | --- | --- | --- | --- |
| `id` | `UUID` | ✅ | `gen_random_uuid()` | 物理主键 |
| `builtin_agent_id` | `UUID` | ✅ | - | 归属内置 Agent（FK -> `builtin_agents.id`，on delete cascade） |
| `version` | `INT` | ✅ | - | 版本号（同 Agent 内唯一） |
| `system_prompt` | `TEXT` | ✅ | - | 系统提示词 |
| `description` | `VARCHAR(255)` | - | - | 变更说明 |
| `is_default` | `BOOLEAN` | ✅ | `false` | 是否默认版本（同 Agent 下最多一个 true） |
| `created_by` | `UUID` | - | - | 创建人（FK -> `user.id`，on delete set null） |
| `created_at` | `TIMESTAMP` | ✅ | `now()` | 创建时间 |
| `metadata` | `JSONB` | ✅ | `{}` | 默认参数/灰度规则（如 temperature、rollout） |

约束：
* `UNIQUE uq_builtin_agent_prompt_versions_agent_version (builtin_agent_id, version)`

索引：
* `UNIQUE INDEX uq_builtin_agent_prompt_versions_default (builtin_agent_id) WHERE is_default`
* `INDEX idx_builtin_agent_prompt_versions_agent (builtin_agent_id)`
* `INDEX idx_builtin_agent_prompt_versions_created_at (created_at)`

---

### 3.19 内置专家用户版本覆盖表 (builtin_agent_user_overrides)

说明：用于 A/B 测试或问题回滚时，为单个用户强制指定某个内置 Agent 的提示词版本。\n

| **字段名** | **类型** | **必填** | **默认值** | **描述/约束** |
| --- | --- | --- | --- | --- |
| `id` | `UUID` | ✅ | `gen_random_uuid()` | 物理主键 |
| `builtin_agent_id` | `UUID` | ✅ | - | 内置 Agent（FK -> `builtin_agents.id`，on delete cascade） |
| `user_id` | `UUID` | ✅ | - | 用户（FK -> `user.id`，on delete cascade） |
| `version` | `INT` | ✅ | - | 强制版本号 |
| `created_at` | `TIMESTAMP` | ✅ | `now()` | 创建时间 |

约束：
* `UNIQUE uq_builtin_agent_user_overrides_agent_user (builtin_agent_id, user_id)`

索引：
* `INDEX idx_builtin_agent_user_overrides_user (user_id)`

---

### 3.20 标准场景表 (scenes)

说明：系统级标准 Scene 的元数据表（只读/不可变的核心逻辑不在 DB）。\n

| **字段名** | **类型** | **必填** | **默认值** | **描述/约束** |
| --- | --- | --- | --- | --- |
| `id` | `UUID` | ✅ | `gen_random_uuid()` | 物理主键 |
| `scene_code` | `VARCHAR(64)` | ✅ | - | 场景编码（如 `script_split`），唯一 |
| `name` | `VARCHAR(128)` | ✅ | - | 展示名称 |
| `type` | `VARCHAR(32)` | ✅ | - | 类型（`chat/process/analysis` 等） |
| `description` | `TEXT` | - | - | 描述 |
| `builtin_agent_id` | `UUID` | - | - | 默认绑定内置 Agent（FK -> `builtin_agents.id`，on delete set null） |
| `required_tools` | `JSONB` | ✅ | `[]` | 依赖工具列表（展示用） |
| `input_schema` | `JSONB` | ✅ | `{}` | 输入 schema（展示/校验用） |
| `output_schema` | `JSONB` | ✅ | `{}` | 输出 schema（展示/校验用） |
| `ui_config` | `JSONB` | ✅ | `{}` | 前端组件配置 |
| `created_at` | `TIMESTAMP` | ✅ | `now()` | 创建时间 |
| `updated_at` | `TIMESTAMP` | ✅ | `now()` | 更新时间 |

约束：
* `UNIQUE uq_scenes_scene_code (scene_code)`

索引：
* `INDEX idx_scenes_type (type)`
* `INDEX idx_scenes_builtin_agent (builtin_agent_id)`

---

### 3.21 用户自定义 Agent 表 (user_agents)

说明：租户级（用户/团队）自定义 Agent。可选继承某个内置专家（`base_builtin_agent_id`）。\n

| **字段名** | **类型** | **必填** | **默认值** | **描述/约束** |
| --- | --- | --- | --- | --- |
| `id` | `UUID` | ✅ | `gen_random_uuid()` | 物理主键 |
| `user_id` | `UUID` | ✅ | - | 创建人（FK -> `user.id`，on delete cascade） |
| `workspace_id` | `UUID` | - | - | 归属工作区/团队（FK -> `workspaces.id`，on delete cascade） |
| `agent_code` | `VARCHAR(64)` | - | - | 可选业务编码（对外展示/导出用） |
| `name` | `VARCHAR(128)` | ✅ | - | 名称 |
| `description` | `TEXT` | - | - | 描述 |
| `base_builtin_agent_id` | `UUID` | - | - | 继承内置 Agent（FK -> `builtin_agents.id`，on delete set null） |
| `system_prompt` | `TEXT` | ✅ | - | 系统提示词 |
| `ai_model_config_id` | `UUID` | - | - | 模型配置（FK -> `ai_model_configs.id`，on delete set null） |
| `temperature` | `NUMERIC` | - | - | 温度 |
| `tools` | `JSONB` | ✅ | `[]` | 工具列表 |
| `is_public` | `BOOLEAN` | ✅ | `false` | 是否对团队公开 |
| `created_at` | `TIMESTAMP` | ✅ | `now()` | 创建时间 |
| `updated_at` | `TIMESTAMP` | ✅ | `now()` | 更新时间 |

索引：
* `INDEX idx_user_agents_user (user_id)`
* `INDEX idx_user_agents_workspace (workspace_id)`
* `INDEX idx_user_agents_public (is_public)`

---

### 3.22 用户自定义应用表 (user_apps)

说明：租户级自定义 App，核心为 `flow_definition`（DAG JSON）。\n

| **字段名** | **类型** | **必填** | **默认值** | **描述/约束** |
| --- | --- | --- | --- | --- |
| `id` | `UUID` | ✅ | `gen_random_uuid()` | 物理主键 |
| `user_id` | `UUID` | ✅ | - | 创建人（FK -> `user.id`，on delete cascade） |
| `workspace_id` | `UUID` | - | - | 归属工作区/团队（FK -> `workspaces.id`，on delete cascade） |
| `name` | `VARCHAR(128)` | ✅ | - | 名称 |
| `description` | `TEXT` | - | - | 描述 |
| `icon` | `VARCHAR(128)` | - | - | 图标标识 |
| `flow_definition` | `JSONB` | ✅ | `{}` | 流程定义（DAG） |
| `trigger_type` | `VARCHAR(32)` | ✅ | `'manual'` | 触发方式 |
| `input_template` | `JSONB` | ✅ | `{}` | 输入表单配置 |
| `output_template` | `JSONB` | ✅ | `{}` | 输出模板 |
| `is_active` | `BOOLEAN` | ✅ | `true` | 是否启用 |
| `created_at` | `TIMESTAMP` | ✅ | `now()` | 创建时间 |
| `updated_at` | `TIMESTAMP` | ✅ | `now()` | 更新时间 |

索引：
* `INDEX idx_user_apps_user (user_id)`
* `INDEX idx_user_apps_workspace (workspace_id)`
* `INDEX idx_user_apps_active (is_active)`

## 4. 枚举字典 (Enumeration Dictionary)

### 4.1 系统权限建议集（示例）

* `system.users`: 用户管理（用户列表、分配角色）
* `system.roles`: 角色与权限管理（维护权限字典、角色授权）
* `system.audit`: 审计日志查看

### 4.2 异步任务状态 (tasks.status)

* `queued`: 已提交，等待执行
* `running`: 执行中
* `succeeded`: 已完成
* `failed`: 已失败
* `canceled`: 已取消

---

## 5. 扩展与变更记录 (Changelog)

| **版本** | **日期** | **修改人** | **变更内容** |
| --- | --- | --- | --- |
| v3.0 | 2026-02-06 | Architect | 新增 RBAC（角色/权限）与审计日志相关表。 |
| v3.1 | 2026-02-07 | Architect | 新增 scripts 表，用于剧本原文件的 MinIO 存储与下载。 |
| v3.2 | 2026-02-07 | Architect | episodes 增加 script_full_text 字段；新增 asset_bindings 支持多层级资产绑定。 |
| v3.3 | 2026-02-07 | Architect | 预留：旧 LLM 中台相关表（已弃用）。 |
| v3.4 | 2026-02-08 | Architect | 新增通用异步任务表（tasks/task_events），用于任务回溯与实时通知。 |
| v3.5 | 2026-02-11 | Architect | 新增积分与 Agent 管理相关表。 |
| v3.6 | 2026-02-11 | Architect | episodes 增加故事板/资产结果根目录字段；新增 file_nodes 用于Markdown文件化结果存储。 |
| v3.7 | 2026-02-12 | Architect | 引入 AI 模型配置与用途绑定（ai_model_configs/ai_model_bindings/ai_usage_events）；Agent 绑定 ai_model_config_id；移除旧 LLM 中台依赖。 |
| v3.8 | 2026-02-12 | Architect | 新增 Agent 平台化表：builtin_agents/提示词版本/用户覆盖/scenes/user_agents/user_apps。 |
