对前端做以下修改


# 剧本创作页面

删除右侧拆解助手栏，将页面设计为2栏 左面分集列表 右侧分集内容

分集不再支持展开

原来 剧本创作 导演分镜 两个按钮进行调整：删除剧本创作 导演分镜按钮。删除所在的横向pannel

在内部的剧集剧本 栏 新增剧集编辑，则可编辑该集剧集文本，markdown编辑器。分场拆解改名故事板拆解  保留资产提取  右侧的齿轮 实现新增（默认插入当前剧集后面），删除剧集，去掉修改和编辑两个菜单

点击提取资产 故事板拆解按钮都是弹出选择系统内置agent对话框，选定后调用这个agent 就行工作，将剧集剧本注入user 提示词 通过任务系统执行。去掉预览落库，任务返回的若符合格式要求直接落库。其他情况则任务失败。后续的由ai agent返回的文本内容 都需要通过markdown 的分隔符来代表多个对象

剧集剧本下面是故事板结果 和 资产结果

全面修改故事板存储的数据结构，故事板将采用markdown格式的独立文件存储，以应对ai提取故事板的格式不稳定，数据库存储的meta数据只保持基本信息即可，前端呈现形式就是一个一个的md文档（模拟），关联资产要以模拟文件夹（资产类别分组）的模式呈现，支持逐级打开，新建、删除
