深入分析当前剧本的数据组织形式，考虑一个具体的需求

目前剧本的结构化是通过ai agent来实现的，但是本项目的ai agent 组织编排能力一般，因此我想在后端暴露一套接口

实现剧本的创建、剧集的创建、剧本资产创建，剧本剧集的故事板创建。

鉴权采用jwt 由管理端签发api key （这个key 在数据库中明文存储即可）

提供restful api 提供swagger doc

软件或者agent 可以通过mcp 或脚本的方式将由外部处理的剧本导入到系统中

你分析一下这个思路的可行性？


此外，我还需要一个接口用来创建批量任务。这个应用来自于 如果我通过三方软件创建了剧本的资产，例如角色资产，三方还可以通过这个接口创建关于这个角色的生成图片的任务。
前端要提供一个入口用来查看这些任务清单，支持批量选择模型。批量启动

这些启动的任务应该被任务系统 多线程执行

获得的图片结果应该和资产绑定

剧本中的资产又称为主体，提示词中使用@+主体名 则代表要使用参考图，这个生成任务一定要引用这个资产的图片（如有），在查看时要能显示被引用的主体的缩略图
